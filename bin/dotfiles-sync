#!/bin/sh
DOTFILES=${DOTFILES:="$HOME/.files"}
for file in $(ls -a $DOTFILES)
do
    file=$(basename $file)
    if grep -Fxq $file $DOTFILES/include.txt
    then
        echo "Creating symlink to $file in $HOME."
    	ln -sf "$DOTFILES/$file" "$HOME/.$file"
    fi
done
